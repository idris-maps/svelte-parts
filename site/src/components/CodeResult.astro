<div class="result">
  <p>Result</p>
  <div class="content">
    <slot></slot>
  </div>
</div>

<style>
  .result {
    margin-top: 1em;
    margin-bottom: 1em;
  }

  .result p {
    font-size: calc(var(--font-size) * 0.6);
    width: 100%;
    text-align: center;
    color: var(--color-border);
    margin: 0;
  }

  .result .content {
    border: dotted 1px var(--color-border);
    border-radius: 1em;
    padding: 1em;
  }
</style>